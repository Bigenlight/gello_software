# Jazzy runtime on junhyeong (2026-09-17)

This is a different PC from the older Humble/laptop3 runbooks.

## Persistent network

`Wired connection 2` binds USB Ethernet `enx00e04c3600bd` to MAC
`00:E0:4C:36:00:BD`, with `192.168.10.100/24`, no gateway, no default route,
and IPv6 disabled. Autoconnect is enabled. The lab NIC remains separate.
`setup_ur7e_ethernet.sh --check` verifies the saved profile and robot connectivity.
UR7e `192.168.10.11` passed ping and TCP 29999/30001/30004 checks.

## Cameras

- cam1 (scene): D435 `143322071682`.
- cam2 (wrist): D435 `143322072540`.
- Both enumerate over USB 3. Use `../launch_cameras.sh --check` for identity checks.
- Install `ros-jazzy-compressed-image-transport` for the native JPEG publishers.
  Raw images can work while the policy's compressed topics have no publisher.
- `--check` verifies identity, not frame delivery. Normal launch checks fresh frames.
- `ros-jazzy-compressed-depth-image-transport` is also required for compressed depth.

After installing the JPEG plugin, both native compressed streams passed: cam1
30.040 Hz, cam2 30.014 Hz. A 600-frame cam1 sample measured 29.535 Hz. No separate
republisher is needed. The normal launcher keeps the two streams running.

## IFQL and tool communication

The augmented carrot checkpoint `ifql_real_k0.9_lead6_aug8_p1.0_s0` at step
100000 was downloaded and its SHA256 matched the local release manifest.
BC initialized with K=1 and the arm completed the start-pose handshake in HOLD.
Autonomous execution has not been started.

The installed URCL tool helper uses `#!/usr/bin/env python3`. An active conda
base selected Python 3.13, which could not import `pytest`; no `/tmp/ttyUR` was
created. The IFQL launcher now prioritizes system Python for ROS children and
checks the helper dependency. IFQL still uses its separate absolute interpreter.
After restart, the helper and socat started successfully.

Gripper operation remains unresolved: both shared PTY and sole direct TCP
attempts returned no Modbus status bytes, including a retry after local stale
connections cleared. Tool feedback reported 24 V and RUNNING. Application Modbus
code is unchanged from the Humble branch, and driver tool parameters match.
This does not establish a hardware fault. Forwarder connection handling and
remote client ownership remain unverified. Local `ss` cannot rule out another
PC owning the robot-side connection. No successful open/close test is claimed.
`check_robotiq_toolcomm.sh` inspects local prerequisites without opening port 54321.
