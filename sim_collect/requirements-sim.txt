# sim_collect — MuJoCo side (.venv, Python 3.11). Pinned to what laptop3 verified on 2026-09-15.
#   uv venv --python 3.11 .venv
#   uv pip install --python .venv/bin/python -r sim_collect/requirements-sim.txt
#   uv pip install --python .venv/bin/python -e . -e third_party/DynamixelSDK/python
# Also needed: git submodules third_party/mujoco_menagerie (UR5e meshes + Robotiq 2F-85) and
# third_party/DynamixelSDK (only for the physical GELLO; eval does not need it).
mujoco==3.10.0
dm-control==1.0.43
glfw==2.10.0
pyopengl==3.1.10
numpy==2.3.5
scipy==1.17.1
pyzmq==27.1.0
h5py==3.16.0
opencv-python-headless==5.0.0.93
pyyaml==6.0.3
tyro==1.0.15
omegaconf==2.3.0
pyserial==3.5
pytest==9.1.1
